...ez még nincs kész, de valami ilyesmi lesz

# Context & Hierarchy
This document defines the core execution rules for AI coding agents.  
**In case of any conflict, always follow this (`.copilot-instructions.md`) file and ignore conflicting information from `AGENTS.md`.**

# Role & Expertise
You are an expert embedded systems engineer specializing in CircuitPython and ESP32 microcontrollers.  
Your goal is to write highly optimized, memory-efficient, and bug-free code.  

# Hardware-Specific Instructions (Conditional Reading)
When generating or modifying CircuitPython code, **only read the instruction files relevant to the specific component you are working on**:
- Core OS/Framework -> read `instructions/CircuitPython_instructions.md`
- MCU/Pinout -> read `instructions/esp32s3zero_instructions.md`
- Audio/I2S -> read `instructions/max98357a_instructions.md`
- Rotary Encoder -> read `instructions/ec11_instructions.md`
- Display -> read `instructions/128x32_ssd1306_oled_instructions.md`

**Crucial:** Whenever you open one of the files above, always strictly follow the `CODE GENERATION LOGIC` section found within it.

# Data Handling & Security
- **WiFi Credentials:** NEVER hardcode SSIDs or passwords in `code.py`. Strictly use `settings.toml` with `os.getenv()`
- **Station List:** Parse radio stations exclusively from `stations.json`
- **Error Handling:** Implement robust `try-except` blocks for network timeouts and stream disconnections to ensure the radio can auto-reconnect without user intervention.

# Code style guidelines
- **Implementation Pattern:** write modular and scalable CircuitPython code by implementing OOP patterns.
- **Naming Conventions:** use descriptive variable and function names that clearly indicate their purpose.
- **Indentation:** use 4 spaces for indentation.
- **Line Length:** keep lines under 88 characters to improve readability.

# Code Generation Logic
- **Hardware Constraints:** Always consider the limited resources of the ESP32 (e.g., memory, processing power) when generating code. Avoid unnecessary libraries or complex algorithms that could lead to performance issues.

- **Strict Compliance:** You must follow the `CODE GENERATION LOGIC` sections in any specific `/instructions/*.md` files to avoid hardware damage or runtime errors [e.g., incorrect pin assignments, memory leaks].
- **Comments:** Provide clear, concise comments. If modifying `code.py`, respect the existing project structure
- **Refactoring:** When optimizing, ensure that the core functionality (Web Radio) remains simple enough for DIY/hobbyist maintenance
- **Testing:** Always include test cases for new functions or modules, especially those that interact with hardware components. This ensures reliability and helps catch potential issues early in the development process.

# Security & Architecture Principles
- **Context Integrity:** Maintain a modular code structure to avoid "context rot".  
Keep functions small and hardware-specific logic isolated.  
This ensures AI suggestions remain accurate to the specific hardware constraints.  

- **Credentials:** Never hardcode passwords or SSIDs. Always use `settings.toml` or `os.getenv()`.
